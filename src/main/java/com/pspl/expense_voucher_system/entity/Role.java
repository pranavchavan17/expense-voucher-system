package com.pspl.expense_voucher_system.entity;

/**
 * Role defines the three application roles allowed in Module 2.
 * Public registration always creates EMPLOYEE users only.
 */
public enum Role {
	EMPLOYEE,
	DIRECTOR,
	ACCOUNTS
}
